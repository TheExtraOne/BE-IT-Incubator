import { LIKE_STATUS } from "../../common/settings";

type TCommentsLikeInputModel = {
  /**
   * likeStatus of the comment
   */
  likeStatus: LIKE_STATUS;
};

export default TCommentsLikeInputModel;
